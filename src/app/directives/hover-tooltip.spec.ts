import { HoverTooltip } from './hover-tooltip';

describe('HoverTooltip', () => {
  it('should create an instance', () => {
    const directive = new HoverTooltip();
    expect(directive).toBeTruthy();
  });
});
